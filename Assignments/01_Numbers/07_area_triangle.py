"""
Write a Python program that will accept the base and height of a triangle and compute its area.
"""

height = float(input("Enter height of triange (in cm): ").strip())
base = float(input("Enter base of triange (in cm): ").strip())

print("Area of Triangle = ", height * base , "cm. sq." )